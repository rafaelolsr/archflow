# /archflow-slides

Analyze the current codebase and generate an architecture slide deck.

Produces a scroll-snap HTML presentation with the animated architecture diagram
as a hero slide, plus supporting slides for components, data flow, external
services, and insights. Navigate with arrow keys, Page Up/Down, or swipe.

Uses the archflow skill with slide deck workflow.
