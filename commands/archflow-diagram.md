# /archflow-diagram

Analyze the current codebase and generate an animated HTML architecture diagram.

Diagram-only mode: produces just the animated diagram without the full report.
Self-contained HTML file with zero external dependencies.

Uses the archflow skill with diagram-only workflow.
